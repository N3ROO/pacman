name: General CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: run CI script
      run: |
        sudo apt-get install python-tk python3-tk tk-dev
        pyenv install 3.5.0
        pip install -r requirements.txt
        ./.ci/run.sh
