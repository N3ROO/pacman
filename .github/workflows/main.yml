name: General CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: run CI script
      run: |
        sudo apt-get install python-tk python3-tk tk-dev python3.5
        python -m pip install -r requirements.txt
        ./.ci/run.sh
